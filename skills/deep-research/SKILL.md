---
name: deep-research
description: Hourglass evidence morph research OS with Expand-Disconfirm-Contract under LRR. Systematic harvest, ACH, consilience, uncertainty calibration, evidence-conditioned re-prompting with logic semantic integrity pattern markers and necessity warrants. Trigger with deep-research on [topic], morph hourglass, Hourglass-EDC, structured deep research, research progress chart, or simple lookup. Light path for quick lookup. Chains from deep-think via morph-shared.
metadata:
  type: workflow
  version: "2.0"
  seals: train_ok-false measured_omega-false G1-OPEN endpointAssumed-false
  pairs-with: morph-shared, deep-think, argueforge, reason-telos-lookup, llmve-matmul-algebra
---

# Deep Research v2 — Hourglass-EDC + LRR

Evidence morph under fixed telos. **Expand** harvest → **Disconfirm** → **Contract_LRR** into a sharper research prompt. Not dump-and-summarize.

Parent controller: **`morph-shared`**.

## Seals / non-claims

```text
train_ok=false · measured_omega=false · G1=OPEN · endpointAssumed=false
```

Research synthesis ≠ measured_omega / train_ok / G1 closed.

## Paths

| Path | When | Loop |
|------|------|------|
| **light** | simple lookup | 1× expand_external → short CONTRACT_LRR → answer |
| **full hourglass** | contested / high-stakes / progress chart | EDC rounds to saturation |
| **after think** | user steered deep-think first | inherit markers/residue; then EDC |

## Geometry: Hourglass-EDC

```text
each round:
  1. EXPAND  — multi-source harvest under current research_prompt
  2. DISCONFIRM — ≥1 search that could kill leading hypothesis (mandatory)
  3. CONTRACT_LRR — rewrite research_prompt (shorter, evidence-conditioned)
     Logic · Ration markers · Reason warrants only
stop: diagnostic saturation | budget | user stop
```

Sim-backed: disconfirm is load-bearing; expand-only and inductive-only contracts lose.

## Phase map (v1 compatible)

| Phase | Hourglass role |
|-------|----------------|
| 0 Protocol | define research_prompt = telos; hypotheses; inclusion rules; seals |
| 1 Harvest | EXPAND |
| 2 ACH / consilience | score diagnosticity; DISCONFIRM built-in |
| 3 Synthesis | CONTRACT_LRR → may re-EXPAND |
| 4 Stress | ArgueForge hand-off if contested policy/identity |

## Evidence rules

- Provenance on every atom  
- Prefer primary / systematic reviews  
- ACH across 3–6 hypotheses  
- Consilience hotspots vs single-source fragile  
- **Contract:** only evidence with `necessary_because` + `marker` enters morph prompt  

## Substrate / LLMVE mode

When charge is transformer residual / factor:
- Stage-ID optional via `transformer-matmul-geometry`  
- NO LLAMA as product_orch mass proxy  
- claim_class on numeric claims  
- Hand off multiply to `llmve-matmul-algebra`  
- Identity contest to `argueforge`  

## Reality-testing

```text
Reason ⇔ Logic ∧ Semantic integrity ∧ Pattern-under-lock
```

Preference-matrix detector on social/rank topics (see reason-telos-lookup).

## Output package

1. Executive synthesis (calibrated)  
2. ACH / evidence map  
3. Pattern markers + warrants  
4. Morph log of research prompts  
5. Uncertainties + what would change conclusions  
6. Open questions  
7. Seals / non-claims  

## Chain

```text
deep-think → deep-research (this)
this → argueforge
morph-shared mode=shared for combined projection
```

## WHEN NOT TO USE

- Pure introspective morph without evidence need → deep-think  
- Closing residual seals from literature polish  

## Resources

- Parent: `morph-shared`  
- `references/hourglass-edc.md` — EDC beat, light path  
