# LLMVE bridge — substrate → factors
**formula_id:** `MVE-2026-07-14-e_sigma_sub-LPT` 
**Product (locked):**
\[
\Omega = H \cdot \frac{\Lambda \cdot \Phi_E \cdot K_M \cdot P_L}{M_{\mathrm{ph}} \cdot C_d \cdot e^{\sigma_{\mathrm{sub}}} \cdot \tau} \cdot f\!\left(\frac{dT}{dR}\right)
\]
Taxes **unmerged**. Telos only in \(f\).
---
## Map stage → factor
| Substrate | Factor | Notes |
|-----------|--------|-------|
| Softmax geometry F | Φ_E ∝ κF (probe); mesh uses F | not energy identity alone |
| T_tail × F | \(P_L=\hat{P}_L\) | structure capacity |
| Unmapped residual spend | \(M_{\mathrm{ph}}\) | catch-all after named taxes |
| Vocab claim debasement | \(C_d\) | needs labels; else identity 1 |
| Enforcement / path drag free vs forced | \(e^{\sigma_{\mathrm{sub}}}\) | denom only; H-STASI-σ-1 |
| LPT dual-obs hold | \(\tau\) | not step count |
| Installed maps / sites | \(K_M\) | structure present |
| Headroom VRAM/hard_block | \(H\) | buffer class |
| Output vs aim | \(f(dT/dR)\) | **only** judgment |
---
## Three-books
| Book | Contents |
|------|----------|
| ENERGY/EXERGY | usable-for-W functional; ore inventory sensors feed capacity geometry |
| Claim medium | \(C_d\); V as currency |
| Path drag | \(e^{\sigma_{\mathrm{sub}}}\) only |
---
## Phantom vs dilution (hard)
- **\(M_{\mathrm{ph}}\)** = unmapped exergy depletion (spend without usable-W map). 
- **\(C_d\)** = claim-medium debasement (too many equal/low-purity claims on V). 
- Volume of false claims can be phantom or C_d-regime; **defense cost** of that volume is \(\sigma\).
---
## Dual
Dual = **theory lanes** (A wins / B wins / synthesis / abandon). 
Not dual-proof gate for .
---
## Post-product mode (default)
1. Forward already ran (product_orch). 
2. Compute F, T_tail, P_L from mass. 
3. Φ_E, M_ph from structure (and energy if present). 
4. Missing tax → identity 1 + status. 
5. Never invent green.
See `artifacts/LLMVE_Validated_Matmul_Algebra_SSOT.md` and skill `llmve-matmul-algebra`.
